{
  "name": "openuvalue",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "Open-source browser-based building-envelope calculator (U-value, temperature profile, condensation risk)",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "test": "pnpm -r --if-present test",
    "typecheck": "pnpm -r --if-present typecheck",
    "build": "pnpm -r --if-present build",
    "dev": "pnpm --filter @openuvalue/web dev"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
